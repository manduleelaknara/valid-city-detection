version: '3.8'

services:
  city-validator:
    build: .
    container_name: city_app_container
    ports:
      - "8080:80"
    networks:
      - app-network
    restart: always

networks:
  app-network:
    driver: bridge